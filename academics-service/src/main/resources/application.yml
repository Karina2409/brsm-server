server:
  port: 8183

#TODO: поменять порт на 8083

spring:
  application:
    name: academics-service

  datasource:
    url: jdbc:postgresql://localhost:5432/core_academics
    username: postgres
    password: p@sSw0rD
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka